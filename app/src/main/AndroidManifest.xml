<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/temporizador_icono_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/temporizador_icono_app_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TemporizadorChess"
        tools:targetApi="31">
        <activity
            android:name=".InformeResult"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="false" />
        <activity
            android:name=".Temporizador"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"> <!-- android:theme="@style/SplashTheme"> -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>